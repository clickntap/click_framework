<bean cache="catalog_bookset">
    <validation>
      <group name="create,update"><![CDATA[
      ]]></group>
      <group name="create"><![CDATA[
      ]]></group>
    </validation>
    <read name="id"><![CDATA[
        select
          id as "id",
          position as "position",
          is_active as "isActive",
          channel as "channel",
          book_id as "bookId",
          creation_date as "creationDate",
          last_modified as "lastModified"
        from
          io_bookset
        where
          id = ${this.id}
    ]]></read>

    <read name="position"><![CDATA[
      select
        id as "id"
      from
        io_bookset
      where
        lower(position) = lower(${this.position})
    ]]></read><read name="is_active"><![CDATA[
      select
        id as "id"
      from
        io_bookset
      where
        lower(is_active) = lower(${this.isActive})
    ]]></read><read name="channel"><![CDATA[
      select
        id as "id"
      from
        io_bookset
      where
        lower(channel) = lower(${this.channel})
    ]]></read>
    <update><![CDATA[
        update io_bookset
        set
          position = ${this.position},
          is_active = ${this.isActive},
          channel = ${this.channel},
          book_id = ${this.bookId},
          last_modified = ${this.now()}
        where
          id = ${this.id}
    ]]></update>
    <execute name="touch"><![CDATA[
        update io_bookset
        set
          last_modified = ${this.now()}
        where
          id = ${this.id}
    ]]></execute>
    <create><![CDATA[
        insert into io_bookset (
          [#if this.bean.id??]id,[/#if]
          position,
          is_active,
          channel,
          book_id,
          creation_date,
          last_modified
        ) values (
          [#if this.bean.id??]${this.id},[/#if]
          ${this.position},
          ${this.isActive},
          ${this.channel},
          ${this.bookId},
          [#if this.bean.creationDate??]${this.creationDate}[#else]${this.now()}[/#if],
          [#if this.bean.lastModified??]${this.lastModified}[#else]${this.now()}[/#if]
        )
    ]]></create>
    <delete><![CDATA[
      delete from
        io_bookset
      where
        id = ${this.id}
    ]]></delete>
    <curr-val><![CDATA[
        select LAST_INSERT_ID() as id
    ]]></curr-val>
</bean>
