<bean cache="app_labellocale">
    <validation>
      <group name="create,update"><![CDATA[
        ${this.assertNotEmpty("text")}
        ${this.assertNotEmpty("labelId")}
        ${this.assertNotEmpty("languageId")}
      ]]></group>
      <group name="create"><![CDATA[
      ]]></group>
    </validation>
    <read name="id"><![CDATA[
        select
          id as "id",
          text as "text",
          label_id as "labelId",
          language_id as "languageId",
          creation_date as "creationDate",
          last_modified as "lastModified"
        from
          io_labellocale
        where
          id = ${this.id}
    ]]></read>

    <read name="text"><![CDATA[
      select
        id as "id"
      from
        io_labellocale
      where
        lower(text) = lower(${this.text})
    ]]></read>
    <update><![CDATA[
        update io_labellocale
        set
          text = ${this.text},
          label_id = ${this.labelId},
          language_id = ${this.languageId},
          last_modified = ${this.now()}
        where
          id = ${this.id}
    ]]></update>
    <execute name="touch"><![CDATA[
        update io_labellocale
        set
          last_modified = ${this.now()}
        where
          id = ${this.id}
    ]]></execute>
    <create><![CDATA[
        insert into io_labellocale (
          [#if this.bean.id??]id,[/#if]
          text,
          label_id,
          language_id,
          creation_date,
          last_modified
        ) values (
          [#if this.bean.id??]${this.id},[/#if]
          ${this.text},
          ${this.labelId},
          ${this.languageId},
          [#if this.bean.creationDate??]${this.creationDate}[#else]${this.now()}[/#if],
          [#if this.bean.lastModified??]${this.lastModified}[#else]${this.now()}[/#if]
        )
    ]]></create>
    <delete><![CDATA[
      delete from
        io_labellocale
      where
        id = ${this.id}
    ]]></delete>
    <curr-val><![CDATA[
        select LAST_INSERT_ID() as id
    ]]></curr-val>
</bean>
