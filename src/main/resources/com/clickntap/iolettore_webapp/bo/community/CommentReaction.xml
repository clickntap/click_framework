<bean cache="community_commentreaction">
    <validation>
      <group name="create,update"><![CDATA[
        ${this.assertNotEmpty("reaction")}
      ]]></group>
      <group name="create"><![CDATA[
      ]]></group>
    </validation>
    <read name="id"><![CDATA[
        select
          id as "id",
          user_id as "userId",
          comment_id as "commentId",
          reaction as "reaction",
          creation_date as "creationDate",
          last_modified as "lastModified"
        from
          io_commentreaction
        where
          id = ${this.id}
    ]]></read>

    <read name="reaction"><![CDATA[
      select
        id as "id"
      from
        io_commentreaction
      where
        lower(reaction) = lower(${this.reaction})
    ]]></read>
    <update><![CDATA[
        update io_commentreaction
        set
          user_id = ${this.userId},
          comment_id = ${this.commentId},
          reaction = ${this.reaction},
          last_modified = ${this.now()}
        where
          id = ${this.id}
    ]]></update>
    <execute name="touch"><![CDATA[
        update io_commentreaction
        set
          last_modified = ${this.now()}
        where
          id = ${this.id}
    ]]></execute>
    <create><![CDATA[
        insert into io_commentreaction (
          [#if this.bean.id??]id,[/#if]
          user_id,
          comment_id,
          reaction,
          creation_date,
          last_modified
        ) values (
          [#if this.bean.id??]${this.id},[/#if]
          ${this.userId},
          ${this.commentId},
          ${this.reaction},
          [#if this.bean.creationDate??]${this.creationDate}[#else]${this.now()}[/#if],
          [#if this.bean.lastModified??]${this.lastModified}[#else]${this.now()}[/#if]
        )
    ]]></create>
    <delete><![CDATA[
      delete from
        io_commentreaction
      where
        id = ${this.id}
    ]]></delete>
    <curr-val><![CDATA[
        select LAST_INSERT_ID() as id
    ]]></curr-val>
</bean>
