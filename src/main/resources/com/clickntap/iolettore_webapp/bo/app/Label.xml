<bean cache="app_label">
    <validation>
      <group name="create,update"><![CDATA[
        ${this.assertNotEmpty("name")}
      ]]></group>
      <group name="create"><![CDATA[
      ]]></group>
    </validation>
    <read name="id"><![CDATA[
        select
          id as "id",
          name as "name",
          creation_date as "creationDate",
          last_modified as "lastModified"
        from
          io_label
        where
          id = ${this.id}
    ]]></read>
    <read-list name="labellocales"><![CDATA[
      select id as "id" from io_labellocale where label_id = ${this.id}
    ]]></read-list>

    <read name="name"><![CDATA[
      select
        id as "id"
      from
        io_label
      where
        lower(name) = lower(${this.name})
    ]]></read>
    <update><![CDATA[
        update io_label
        set
          name = ${this.name},
          last_modified = ${this.now()}
        where
          id = ${this.id}
    ]]></update>
    <execute name="touch"><![CDATA[
        update io_label
        set
          last_modified = ${this.now()}
        where
          id = ${this.id}
    ]]></execute>
    <create><![CDATA[
        insert into io_label (
          [#if this.bean.id??]id,[/#if]
          name,
          creation_date,
          last_modified
        ) values (
          [#if this.bean.id??]${this.id},[/#if]
          ${this.name},
          [#if this.bean.creationDate??]${this.creationDate}[#else]${this.now()}[/#if],
          [#if this.bean.lastModified??]${this.lastModified}[#else]${this.now()}[/#if]
        )
    ]]></create>
    <delete><![CDATA[
      delete from
        io_label
      where
        id = ${this.id}
    ]]></delete>
    <curr-val><![CDATA[
        select LAST_INSERT_ID() as id
    ]]></curr-val>
</bean>
