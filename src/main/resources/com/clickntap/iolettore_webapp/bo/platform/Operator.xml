<bean cache="platform_operator">
    <validation>
      <group name="create,update"><![CDATA[
        ${this.assertNotEmpty("name")}
        ${this.assertNotEmpty("surname")}
        ${this.assertNotEmpty("email")}
        ${this.assertNotEmpty("username")}
        ${this.assertNotEmpty("password")}
        ${this.assertEmail("email")}
        ${this.assertUnique("email")}
        ${this.assertUnique("username")}
      ]]></group>
      <group name="create"><![CDATA[
        ${this.assertNotEmpty("confirmPassword")}
        ${this.assertEquals("confirmPassword","password")}
        ${this.assertLength("password",4,16)}
      ]]></group>
      <group name="execute-password"><![CDATA[
  			${this.assertExists("oldPassword")}
  			${this.assertNotEmpty("oldPassword")}
  			${this.assertNotEmpty("newPassword")}
  			${this.assertEquals("confirmNewPassword","newPassword")}
  			${this.assertLength("newPassword",4,16)}
  		]]></group>
    </validation>
    <read name="id"><![CDATA[
        select
          id as "id",
          name as "name",
          surname as "surname",
          email as "email",
          username as "username",
          password as "password",
          creation_date as "creationDate",
          last_modified as "lastModified"
        from
          io_operator
        where
          id = ${this.id}
    ]]></read>
    <read-list name="email"><![CDATA[
    		select id as "id" from io_operator where email = ${this.email} and (id != ${this.id} or ${this.id} is null)
    ]]></read-list>
    <read name="auth"><![CDATA[
    		select id as "id" from io_operator where (lower(username) = lower(${this.email}) or lower(email) = lower(${this.email})) and password = ${this.passwordMD5} limit 1
    ]]></read>
    <read-list name="oldPassword"><![CDATA[
    		select id as "id" from io_operator where password = ${this.oldPasswordMD5} and id = ${this.id}
    ]]></read-list>
    <read-list name="username"><![CDATA[
    		select id as "id" from io_operator where username = ${this.username} and (id != ${this.id} or ${this.id} is null)
    ]]></read-list>
    <execute name="password"><![CDATA[
      update io_operator set password = ${this.newPasswordMD5}, last_modified = ${this.now()} where id = ${this.id}
    ]]></execute>
    <execute name="forgot-password"><![CDATA[
      update io_operator set password = ${this.newPasswordMD5}, last_modified = ${this.now()} where id = ${this.id}
    ]]></execute>

    <read name="name"><![CDATA[
      select
        id as "id"
      from
        io_operator
      where
        lower(name) = lower(${this.name})
    ]]></read><read name="surname"><![CDATA[
      select
        id as "id"
      from
        io_operator
      where
        lower(surname) = lower(${this.surname})
    ]]></read><read name="email"><![CDATA[
      select
        id as "id"
      from
        io_operator
      where
        lower(email) = lower(${this.email})
    ]]></read><read name="username"><![CDATA[
      select
        id as "id"
      from
        io_operator
      where
        lower(username) = lower(${this.username})
    ]]></read>
    <update><![CDATA[
        update io_operator
        set
          name = ${this.name},
          surname = ${this.surname},
          email = ${this.email},
          username = ${this.username},
          password = ${this.password},
          last_modified = ${this.now()}
        where
          id = ${this.id}
    ]]></update>
    <execute name="touch"><![CDATA[
        update io_operator
        set
          last_modified = ${this.now()}
        where
          id = ${this.id}
    ]]></execute>
    <create><![CDATA[
        insert into io_operator (
          [#if this.bean.id??]id,[/#if]
          name,
          surname,
          email,
          username,
          password,
          creation_date,
          last_modified
        ) values (
          [#if this.bean.id??]${this.id},[/#if]
          ${this.name},
          ${this.surname},
          ${this.email},
          ${this.username},
          ${this.passwordMD5},
          [#if this.bean.creationDate??]${this.creationDate}[#else]${this.now()}[/#if],
          [#if this.bean.lastModified??]${this.lastModified}[#else]${this.now()}[/#if]
        )
    ]]></create>
    <delete><![CDATA[
      delete from
        io_operator
      where
        id = ${this.id}
    ]]></delete>
    <curr-val><![CDATA[
        select LAST_INSERT_ID() as id
    ]]></curr-val>
</bean>
