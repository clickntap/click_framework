<bean cache="community_postreaction">
    <validation>
      <group name="create,update"><![CDATA[
        ${this.assertNotEmpty("reaction")}
      ]]></group>
      <group name="create"><![CDATA[
      ]]></group>
    </validation>
    <read name="id"><![CDATA[
        select
          id as "id",
          user_id as "userId",
          post_id as "postId",
          reaction as "reaction",
          creation_date as "creationDate",
          last_modified as "lastModified"
        from
          io_postreaction
        where
          id = ${this.id}
    ]]></read>

    <read name="reaction"><![CDATA[
      select
        id as "id"
      from
        io_postreaction
      where
        lower(reaction) = lower(${this.reaction})
    ]]></read>
    <update><![CDATA[
        update io_postreaction
        set
          user_id = ${this.userId},
          post_id = ${this.postId},
          reaction = ${this.reaction},
          last_modified = ${this.now()}
        where
          id = ${this.id}
    ]]></update>
    <execute name="touch"><![CDATA[
        update io_postreaction
        set
          last_modified = ${this.now()}
        where
          id = ${this.id}
    ]]></execute>
    <create><![CDATA[
        insert into io_postreaction (
          [#if this.bean.id??]id,[/#if]
          user_id,
          post_id,
          reaction,
          creation_date,
          last_modified
        ) values (
          [#if this.bean.id??]${this.id},[/#if]
          ${this.userId},
          ${this.postId},
          ${this.reaction},
          [#if this.bean.creationDate??]${this.creationDate}[#else]${this.now()}[/#if],
          [#if this.bean.lastModified??]${this.lastModified}[#else]${this.now()}[/#if]
        )
    ]]></create>
    <delete><![CDATA[
      delete from
        io_postreaction
      where
        id = ${this.id}
    ]]></delete>
    <curr-val><![CDATA[
        select LAST_INSERT_ID() as id
    ]]></curr-val>
</bean>
