<bean cache="catalog_bookandget">
    <validation>
      <group name="create,update"><![CDATA[
        ${this.assertNotEmpty("startDateOnlyDate")}
        ${this.assertNotEmpty("startDateOnlyTime")}
        ${this.assertNotEmpty("endDateOnlyDate")}
        ${this.assertNotEmpty("endDateOnlyTime")}
        ${this.assertNotEmpty("userId")}
        ${this.assertNotEmpty("bookId")}
        ${this.assertNotEmpty("shopId")}
        ${this.assertNotEmpty("state")}
      ]]></group>
      <group name="create"><![CDATA[
      ]]></group>
    </validation>
    <read name="id"><![CDATA[
        select
          id as "id",
          start_date as "startDate",
          end_date as "endDate",
          user_id as "userId",
          book_id as "bookId",
          shop_id as "shopId",
          state as "state",
          creation_date as "creationDate",
          last_modified as "lastModified"
        from
          io_bookandget
        where
          id = ${this.id}
    ]]></read>
    <read-list name="bookandgethistories"><![CDATA[
      select id as "id" from io_bookandgethistory where book_get_id = ${this.id}
    ]]></read-list>

    <read name="start_date"><![CDATA[
      select
        id as "id"
      from
        io_bookandget
      where
        lower(start_date) = lower(${this.startDate})
    ]]></read><read name="end_date"><![CDATA[
      select
        id as "id"
      from
        io_bookandget
      where
        lower(end_date) = lower(${this.endDate})
    ]]></read>
    <update><![CDATA[
        update io_bookandget
        set
          start_date = ${this.startDate},
          end_date = ${this.endDate},
          user_id = ${this.userId},
          book_id = ${this.bookId},
          shop_id = ${this.shopId},
          state = ${this.state},
          last_modified = ${this.now()}
        where
          id = ${this.id}
    ]]></update>
    <execute name="touch"><![CDATA[
        update io_bookandget
        set
          last_modified = ${this.now()}
        where
          id = ${this.id}
    ]]></execute>
    <create><![CDATA[
        insert into io_bookandget (
          [#if this.bean.id??]id,[/#if]
          start_date,
          end_date,
          user_id,
          book_id,
          shop_id,
          state,
          creation_date,
          last_modified
        ) values (
          [#if this.bean.id??]${this.id},[/#if]
          ${this.startDate},
          ${this.endDate},
          ${this.userId},
          ${this.bookId},
          ${this.shopId},
          coalesce(${this.state},1),
          [#if this.bean.creationDate??]${this.creationDate}[#else]${this.now()}[/#if],
          [#if this.bean.lastModified??]${this.lastModified}[#else]${this.now()}[/#if]
        )
    ]]></create>
    <delete><![CDATA[
      delete from
        io_bookandget
      where
        id = ${this.id}
    ]]></delete>
    <curr-val><![CDATA[
        select LAST_INSERT_ID() as id
    ]]></curr-val>
</bean>
