<bean cache="catalog_event">
    <validation>
      <group name="create,update"><![CDATA[
        ${this.assertNotEmpty("name")}
        ${this.assertNotEmpty("description")}
        ${this.assertNotEmpty("startDateOnlyDate")}
        ${this.assertNotEmpty("startDateOnlyTime")}
        ${this.assertNotEmpty("shopId")}
      ]]></group>
      <group name="create"><![CDATA[
      ]]></group>
    </validation>
    <read name="id"><![CDATA[
        select
          id as "id",
          name as "name",
          location as "location",
          latitude as "latitude",
          longitude as "longitude",
          description as "description",
          start_date as "startDate",
          end_date as "endDate",
          shop_id as "shopId",
          book_id as "bookId",
          is_active as "isActive",
          creation_date as "creationDate",
          last_modified as "lastModified"
        from
          io_event
        where
          id = ${this.id}
    ]]></read>
    <read-list name="links"><![CDATA[
      select id as "id" from io_link where event_id = ${this.id}
    ]]></read-list>
    <read-list name="useractions"><![CDATA[
      select id as "id" from io_useraction where event_id = ${this.id}
    ]]></read-list>

    <read name="name"><![CDATA[
      select
        id as "id"
      from
        io_event
      where
        lower(name) = lower(${this.name})
    ]]></read><read name="description"><![CDATA[
      select
        id as "id"
      from
        io_event
      where
        lower(description) = lower(${this.description})
    ]]></read><read name="is_active"><![CDATA[
      select
        id as "id"
      from
        io_event
      where
        lower(is_active) = lower(${this.isActive})
    ]]></read>
    <update><![CDATA[
        update io_event
        set
          name = ${this.name},
          location = ${this.location},
          latitude = ${this.latitude},
          longitude = ${this.longitude},
          description = ${this.description},
          start_date = ${this.startDate},
          end_date = ${this.endDate},
          shop_id = ${this.shopId},
          book_id = ${this.bookId},
          is_active = ${this.isActive},
          last_modified = ${this.now()}
        where
          id = ${this.id}
    ]]></update>
    <execute name="touch"><![CDATA[
        update io_event
        set
          last_modified = ${this.now()}
        where
          id = ${this.id}
    ]]></execute>
    <create><![CDATA[
        insert into io_event (
          [#if this.bean.id??]id,[/#if]
          name,
          location,
          latitude,
          longitude,
          description,
          start_date,
          end_date,
          shop_id,
          book_id,
          is_active,
          creation_date,
          last_modified
        ) values (
          [#if this.bean.id??]${this.id},[/#if]
          ${this.name},
          ${this.location},
          ${this.latitude},
          ${this.longitude},
          ${this.description},
          ${this.startDate},
          ${this.endDate},
          ${this.shopId},
          ${this.bookId},
          ${this.isActive},
          [#if this.bean.creationDate??]${this.creationDate}[#else]${this.now()}[/#if],
          [#if this.bean.lastModified??]${this.lastModified}[#else]${this.now()}[/#if]
        )
    ]]></create>
    <delete><![CDATA[
      delete from
        io_event
      where
        id = ${this.id}
    ]]></delete>
    <curr-val><![CDATA[
        select LAST_INSERT_ID() as id
    ]]></curr-val>
</bean>
