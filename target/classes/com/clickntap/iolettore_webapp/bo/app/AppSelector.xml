<bean>
  <read-list name="labels"><![CDATA[
    [#assign query = (this.bean.query!"")?lower_case]
    select
      id as "id"
    from io_label
    [#list query?split("[ .;:,]", "r") as word]
    [#if word_index == 0]where[#else]and[/#if]
    (
           lower(name) like ${this.toString("%"+word+"%")}
    
    )
    [/#list]
  ]]></read-list>
  <read-list name="labellocales"><![CDATA[
    [#assign query = (this.bean.query!"")?lower_case]
    select
      id as "id"
    from io_labellocale
    [#list query?split("[ .;:,]", "r") as word]
    [#if word_index == 0]where[#else]and[/#if]
    (
           lower(text) like ${this.toString("%"+word+"%")}
    
    )
    [/#list]
  ]]></read-list>
  
</bean>
