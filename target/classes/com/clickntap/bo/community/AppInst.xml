<bean cache="community_appinst">
    <validation>
      <group name="create,update"><![CDATA[
        ${this.assertNotEmpty("channel")}
      ]]></group>
      <group name="create"><![CDATA[
      ]]></group>
    </validation>
    <read name="id"><![CDATA[
        select
          id as "id",
          user_id as "userId",
          latitude as "latitude",
          longitude as "longitude",
          push_token as "pushToken",
          channel as "channel",
          ip as "ip",
          creation_date as "creationDate",
          last_modified as "lastModified"
        from
          io_appinst
        where
          id = ${this.id}
    ]]></read>

    <read name="push_token"><![CDATA[
      select
        id as "id"
      from
        io_appinst
      where
        lower(push_token) = lower(${this.pushToken})
    ]]></read><read name="channel"><![CDATA[
      select
        id as "id"
      from
        io_appinst
      where
        lower(channel) = lower(${this.channel})
    ]]></read><read name="ip"><![CDATA[
      select
        id as "id"
      from
        io_appinst
      where
        lower(ip) = lower(${this.ip})
    ]]></read>
    <update><![CDATA[
        update io_appinst
        set
          user_id = ${this.userId},
          latitude = ${this.latitude},
          longitude = ${this.longitude},
          push_token = ${this.pushToken},
          channel = ${this.channel},
          ip = ${this.ip},
          last_modified = ${this.now()}
        where
          id = ${this.id}
    ]]></update>
    <execute name="touch"><![CDATA[
        update io_appinst
        set
          last_modified = ${this.now()}
        where
          id = ${this.id}
    ]]></execute>
    <create><![CDATA[
        insert into io_appinst (
          [#if this.bean.id??]id,[/#if]
          user_id,
          latitude,
          longitude,
          push_token,
          channel,
          ip,
          creation_date,
          last_modified
        ) values (
          [#if this.bean.id??]${this.id},[/#if]
          ${this.userId},
          ${this.latitude},
          ${this.longitude},
          ${this.pushToken},
          ${this.channel},
          ${this.ip},
          [#if this.bean.creationDate??]${this.creationDate}[#else]${this.now()}[/#if],
          [#if this.bean.lastModified??]${this.lastModified}[#else]${this.now()}[/#if]
        )
    ]]></create>
    <delete><![CDATA[
      delete from
        io_appinst
      where
        id = ${this.id}
    ]]></delete>
    <curr-val><![CDATA[
        select LAST_INSERT_ID() as id
    ]]></curr-val>
</bean>
