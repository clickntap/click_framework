<bean cache="community_userrating">
    <validation>
      <group name="create,update"><![CDATA[
      ]]></group>
      <group name="create"><![CDATA[
      ]]></group>
    </validation>
    <read name="id"><![CDATA[
        select
          id as "id",
          user_id as "userId",
          book_id as "bookId",
          rating as "rating",
          creation_date as "creationDate",
          last_modified as "lastModified"
        from
          io_userrating
        where
          id = ${this.id}
    ]]></read>

    
    <update><![CDATA[
        update io_userrating
        set
          user_id = ${this.userId},
          book_id = ${this.bookId},
          rating = ${this.rating},
          last_modified = ${this.now()}
        where
          id = ${this.id}
    ]]></update>
    <execute name="touch"><![CDATA[
        update io_userrating
        set
          last_modified = ${this.now()}
        where
          id = ${this.id}
    ]]></execute>
    <create><![CDATA[
        insert into io_userrating (
          [#if this.bean.id??]id,[/#if]
          user_id,
          book_id,
          rating,
          creation_date,
          last_modified
        ) values (
          [#if this.bean.id??]${this.id},[/#if]
          ${this.userId},
          ${this.bookId},
          ${this.rating},
          [#if this.bean.creationDate??]${this.creationDate}[#else]${this.now()}[/#if],
          [#if this.bean.lastModified??]${this.lastModified}[#else]${this.now()}[/#if]
        )
    ]]></create>
    <delete><![CDATA[
      delete from
        io_userrating
      where
        id = ${this.id}
    ]]></delete>
    <curr-val><![CDATA[
        select LAST_INSERT_ID() as id
    ]]></curr-val>
</bean>
