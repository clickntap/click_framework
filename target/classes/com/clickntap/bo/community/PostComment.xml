<bean cache="community_postcomment">
    <validation>
      <group name="create,update"><![CDATA[
        ${this.assertNotEmpty("message")}
      ]]></group>
      <group name="create"><![CDATA[
      ]]></group>
    </validation>
    <read name="id"><![CDATA[
        select
          id as "id",
          message as "message",
          user_id as "userId",
          post_id as "postId",
          parent_id as "parentId",
          creation_date as "creationDate",
          last_modified as "lastModified"
        from
          io_postcomment
        where
          id = ${this.id}
    ]]></read>
    <read-list name="children"><![CDATA[
      select id as "id" from io_postcomment where parent_id = ${this.id}
    ]]></read-list>
    <read-list name="postcommentreactions"><![CDATA[
      select id as "id" from io_postcommentreaction where comment_id = ${this.id}
    ]]></read-list>
    <read-list name="useractions"><![CDATA[
      select id as "id" from io_useraction where post_comment_id = ${this.id}
    ]]></read-list>

    <read name="message"><![CDATA[
      select
        id as "id"
      from
        io_postcomment
      where
        lower(message) = lower(${this.message})
    ]]></read>
    <update><![CDATA[
        update io_postcomment
        set
          message = ${this.message},
          user_id = ${this.userId},
          post_id = ${this.postId},
          parent_id = ${this.parentId},
          last_modified = ${this.now()}
        where
          id = ${this.id}
    ]]></update>
    <execute name="touch"><![CDATA[
        update io_postcomment
        set
          last_modified = ${this.now()}
        where
          id = ${this.id}
    ]]></execute>
    <create><![CDATA[
        insert into io_postcomment (
          [#if this.bean.id??]id,[/#if]
          message,
          user_id,
          post_id,
          parent_id,
          creation_date,
          last_modified
        ) values (
          [#if this.bean.id??]${this.id},[/#if]
          ${this.message},
          ${this.userId},
          ${this.postId},
          ${this.parentId},
          [#if this.bean.creationDate??]${this.creationDate}[#else]${this.now()}[/#if],
          [#if this.bean.lastModified??]${this.lastModified}[#else]${this.now()}[/#if]
        )
    ]]></create>
    <delete><![CDATA[
      delete from
        io_postcomment
      where
        id = ${this.id}
    ]]></delete>
    <curr-val><![CDATA[
        select LAST_INSERT_ID() as id
    ]]></curr-val>
</bean>
