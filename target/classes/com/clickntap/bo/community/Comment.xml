<bean cache="community_comment">
    <validation>
      <group name="create,update"><![CDATA[
        ${this.assertNotEmpty("message")}
      ]]></group>
      <group name="create"><![CDATA[
      ]]></group>
    </validation>
    <read name="id"><![CDATA[
        select
          id as "id",
          message as "message",
          user_id as "userId",
          book_id as "bookId",
          parent_id as "parentId",
          creation_date as "creationDate",
          last_modified as "lastModified"
        from
          io_comment
        where
          id = ${this.id}
    ]]></read>
    <read-list name="children"><![CDATA[
      select id as "id" from io_comment where parent_id = ${this.id}
    ]]></read-list>
    <read-list name="commentreactions"><![CDATA[
      select id as "id" from io_commentreaction where comment_id = ${this.id}
    ]]></read-list>
    <read-list name="useractions"><![CDATA[
      select id as "id" from io_useraction where comment_id = ${this.id}
    ]]></read-list>

    <read name="message"><![CDATA[
      select
        id as "id"
      from
        io_comment
      where
        lower(message) = lower(${this.message})
    ]]></read>
    <update><![CDATA[
        update io_comment
        set
          message = ${this.message},
          user_id = ${this.userId},
          book_id = ${this.bookId},
          parent_id = ${this.parentId},
          last_modified = ${this.now()}
        where
          id = ${this.id}
    ]]></update>
    <execute name="touch"><![CDATA[
        update io_comment
        set
          last_modified = ${this.now()}
        where
          id = ${this.id}
    ]]></execute>
    <create><![CDATA[
        insert into io_comment (
          [#if this.bean.id??]id,[/#if]
          message,
          user_id,
          book_id,
          parent_id,
          creation_date,
          last_modified
        ) values (
          [#if this.bean.id??]${this.id},[/#if]
          ${this.message},
          ${this.userId},
          ${this.bookId},
          ${this.parentId},
          [#if this.bean.creationDate??]${this.creationDate}[#else]${this.now()}[/#if],
          [#if this.bean.lastModified??]${this.lastModified}[#else]${this.now()}[/#if]
        )
    ]]></create>
    <delete><![CDATA[
      delete from
        io_comment
      where
        id = ${this.id}
    ]]></delete>
    <curr-val><![CDATA[
        select LAST_INSERT_ID() as id
    ]]></curr-val>
</bean>
